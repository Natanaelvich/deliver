import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#eee',
    borderRadius: 15,
  },
  containerPedido: {
    backgroundColor: '#fff',
    borderRadius: 30,
    margin: 12,
    padding: 20,
  },
  txtPedido: {
    fontSize: 15,
    fontWeight: 'bold',
    color: '#999',
  },
});

export default styles;
